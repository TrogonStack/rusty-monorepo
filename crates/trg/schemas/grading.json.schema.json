{
  "$defs": {
    "AssertionGradeResult": {
      "properties": {
        "assertion": {
          "minLength": 1,
          "type": "string"
        },
        "evidence": {
          "type": "string"
        },
        "excluded": {
          "description": "Present when the grader presupposes the skill, so its `passed` value is\nan indicator to read rather than a score to count. Kept out of\n`summary.pass_rate` in both arms, so the gap between them measures the\nwork and not the premise.",
          "type": [
            "string",
            "null"
          ]
        },
        "grader": {
          "$ref": "#/$defs/GraderInfo"
        },
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "passed": {
          "type": "boolean"
        },
        "rationale": {
          "type": [
            "string",
            "null"
          ]
        },
        "ungraded": {
          "description": "No grader could answer this assertion at all: it deferred to a judge and\ngrading ran under `--grader none`, so no judge was consulted. Distinct from\n`unsupported`, which means a grader existed and the harness could not answer\nit, and from `excluded`, which means the author scoped it to the other arm.",
          "type": [
            "string",
            "null"
          ]
        },
        "unsupported": {
          "description": "Present when the property cannot be observed on the runner that produced\nthe run. Such a result is neither a pass nor a failure, so it is excluded\nfrom `summary.pass_rate`.",
          "type": [
            "string",
            "null"
          ]
        },
        "votes": {
          "anyOf": [
            {
              "$ref": "#/$defs/JudgeVoteTally"
            },
            {
              "type": "null"
            }
          ],
          "description": "How a panel of judges split, present only when more than one opinion was taken.\nA single opinion has no split to report, and `passed` already carries it."
        },
        "weight": {
          "anyOf": [
            {
              "$ref": "#/$defs/GraderWeight"
            },
            {
              "type": "null"
            }
          ],
          "description": "How much this result counts toward its case's score, relative to the\nrest of the case. Absent whenever the declaring grader left it\nunweighted, so an undeclared weight cannot be told apart in the\nserialized form from a build that predates weighting at all."
        }
      },
      "required": [
        "assertion",
        "passed",
        "evidence",
        "grader"
      ],
      "type": "object"
    },
    "GraderInfo": {
      "properties": {
        "command": {
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "$ref": "#/$defs/GraderKind"
        },
        "model": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "GraderKind": {
      "enum": [
        "mechanical",
        "declarative",
        "llm",
        "script",
        "needs_llm",
        "none"
      ],
      "type": "string"
    },
    "GraderWeight": {
      "description": "How much one grader's result counts toward its case's score, relative to\nevery other grader and free-text assertion in the same case.\n\nAlways positive. A grader worth nothing to the score belongs out of the\ncase entirely, scored with `arm: with_only` rather than weighted to zero,\nand a negative weight has no share of a score to subtract from, so\nneither reading is one this type can hold.",
      "exclusiveMinimum": 0.0,
      "type": "number"
    },
    "GradingSummary": {
      "properties": {
        "excluded": {
          "default": 0,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "failed": {
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "pass_rate": {
          "description": "Over the scored assertions only, so an ungradable property cannot drag a\nskill's score down on a runner that simply cannot be observed. `null` when\nnothing was scored at all, because no assertion answered is not the same\nresult as every assertion failed.",
          "format": "double",
          "maximum": 1.0,
          "minimum": 0.0,
          "type": [
            "number",
            "null"
          ]
        },
        "passed": {
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "total": {
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "ungraded": {
          "default": 0,
          "description": "No grader could even attempt these, so they carry no signal either way.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "unsupported": {
          "default": 0,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "passed",
        "failed",
        "total"
      ],
      "type": "object"
    },
    "JudgeVoteTally": {
      "description": "How a panel of judges split on one assertion.\n\nRecorded so a reader can tell an assertion the judge was sure about from one it was\ndivided on. A divided panel is a statement about the assertion, not about the run: it\nsays the material admits both readings, which is a prompt to write the assertion more\nprecisely rather than to go looking at the skill.",
      "properties": {
        "failed": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "passed": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "passed",
        "failed"
      ],
      "type": "object"
    }
  },
  "$id": "https://trogonstack.dev/schemas/trg/grading.json.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "assertion_results": {
      "items": {
        "$ref": "#/$defs/AssertionGradeResult"
      },
      "type": "array"
    },
    "summary": {
      "$ref": "#/$defs/GradingSummary"
    }
  },
  "required": [
    "assertion_results",
    "summary"
  ],
  "title": "trg skills eval grading",
  "type": "object"
}
