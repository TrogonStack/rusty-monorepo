{
  "$defs": {
    "ConfigHomeBasis": {
      "description": "How a harness's config home was found.",
      "oneOf": [
        {
          "description": "The harness honours this environment variable, so the config home was named\ndirectly rather than derived.",
          "properties": {
            "kind": {
              "const": "variable",
              "type": "string"
            },
            "name": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ],
          "type": "object"
        },
        {
          "description": "The harness has no override; the config home sits at a fixed name under `HOME`.",
          "properties": {
            "kind": {
              "const": "home_relative",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "ConfigHomeOrigin": {
      "description": "Whether a run's config home belonged to the operator or was made for the run.",
      "oneOf": [
        {
          "const": "host",
          "description": "The run saw the operator's own config home, with its skills, instructions, MCP\nservers, and history.",
          "type": "string"
        },
        {
          "const": "run",
          "description": "The config home was created for this run and holds only what an isolated run\nneeds to authenticate.",
          "type": "string"
        }
      ]
    },
    "DeclaredConfigHome": {
      "properties": {
        "basis": {
          "$ref": "#/$defs/ConfigHomeBasis"
        },
        "origin": {
          "$ref": "#/$defs/ConfigHomeOrigin"
        },
        "path": {
          "type": "string"
        }
      },
      "required": [
        "path",
        "origin",
        "basis"
      ],
      "type": "object"
    }
  },
  "$id": "https://trogonstack.dev/schemas/trg/env.json.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "The document written to a run's `env.json`.",
  "properties": {
    "config_home": {
      "anyOf": [
        {
          "$ref": "#/$defs/DeclaredConfigHome"
        },
        {
          "type": "null"
        }
      ],
      "description": "Absent only when the host gave a run neither the harness's override variable nor\na `HOME` to resolve a config home against."
    },
    "vars": {
      "additionalProperties": {
        "type": "string"
      },
      "type": "object"
    }
  },
  "required": [
    "vars"
  ],
  "title": "trg skills eval run environment",
  "type": "object"
}
